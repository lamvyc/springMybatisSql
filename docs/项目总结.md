# sql-learning-platform 项目总结

## 一、项目架构总览

```
浏览器 / Postman
      │ HTTP
      ▼
Controller 层       接收请求、参数校验、返回 Result 统一结构
      │
      ▼
Service 层          业务逻辑编排、事务控制、DTO转Entity
      │
      ▼
Mapper 层           接口定义方法（UserNativeMapper 手写、UserPlusMapper 继承BaseMapper）
      │
      ▼
Mapper XML          原生 MyBatis SQL 定义（动态SQL / ResultMap）
      │
      ▼
MyBatis 框架        预编译、参数映射、结果集映射、SQL日志
      │
      ▼
MySQL 8             执行 SQL、返回结果集
```

| 层          | 作用                      | 本项目的例子                                                 |
|------------|-------------------------|--------------------------------------------------------|
| Controller | 接收HTTP请求、参数绑定、返回JSON    | `EmployeeController`、`AnalysisController`              |
| Service    | 业务逻辑、事务、参数校验、组装VO       | `EmployeeServiceImpl`、`UserServiceImpl`                |
| Mapper接口   | 声明数据库操作（方法签名）           | `EmployeeMapper`、`SqlExecutorMapper`                   |
| Mapper XML | 编写真实SQL、动态SQL、ResultMap | `EmployeeMapper.xml`、`AnalysisMapper.xml`              |
| Entity     | 与数据库表一一对应的实体类           | `Employee`、`Orders`                                    |
| DTO        | 接收前端请求参数（不暴露实体）         | `UserDTO`、`ExecuteSqlRequest`                          |
| VO         | 返回给前端展示的视图对象            | `EmployeeVO`、`ExecuteSqlVO`                            |
| config     | 配置类                     | `MybatisConfig`（@MapperScan）、`MybatisPlusConfig`（分页插件） |

---

## 二、SQL 知识地图

### 1. 基础查询
- `WHERE`：行过滤
- `GROUP BY`：分组聚合，与 `聚合函数` 配合
- `HAVING`：分组后过滤（`WHERE` 过滤行，`HAVING` 过滤分组）
- `ORDER BY`：排序
- `LIMIT offset, size`：分页（全局 TopN）

### 2. 多表连接
| 连接类型         | 含义             | 案例           |
|--------------|----------------|--------------|
| `INNER JOIN` | 只保留两表都匹配的行     | 题1、题2        |
| `LEFT JOIN`  | 保留左表全部行        | 题16（含无员工部门）  |
| 自连接          | 同一张表连接两次       | 题14、15（领导关系） |
| 非等值连接        | `BETWEEN` 范围匹配 | 题3、22（薪资等级）  |

### 3. 子查询
- 标量子查询：`(SELECT AVG(salary) FROM employee)` → 题22
- 表子查询：`FROM (SELECT ... GROUP BY ...) t` → 题1、2
- `ALL` 关键字：`> ALL(...)` → 题25
- `EXISTS / NOT EXISTS`：更安全的多对多判断 → 题35-1

### 4. 窗口函数（阶段6核心）
| 函数             | 用途        | 案例          |
|----------------|-----------|-------------|
| `ROW_NUMBER()` | 分组内唯一序号   | 分组TopN、连续问题 |
| `RANK()`       | 并列排名（跳号）  | 用户消费排行      |
| `DENSE_RANK()` | 并列排名（不跳号） | 面试对比        |

**连续问题通用解法**：`日期 - ROW_NUMBER()` 差值相同即连续
```sql
DATE_SUB(login_date, INTERVAL ROW_NUMBER() OVER(
    PARTITION BY user_id ORDER BY login_date) DAY) AS grp
```

**分组TopN为什么不用LIMIT**：LIMIT 只能做全局排序后截断，无法"按分类分别取前3"。
必须用窗口函数 `PARTITION BY` 分区后在分区内排序。

### 5. 行列转换
`CASE WHEN` + `COUNT/SUM` 条件聚合 → 题45 订单状态分布

---

## 三、MyBatis 知识地图

### 执行流程
```
Mapper接口调用
   → MyBatis 根据 namespace + methodId 找到对应的 SQL
   → 解析参数（#{} 预编译占位，${} 字符串拼接）
   → 执行 SQL（PreparedStatement 预编译，防SQL注入）
   → 结果集映射（resultType 自动映射 / resultMap 手动映射）
   → 返回 Java 对象
```

### 核心概念
| 概念             | 说明                                    | 本项目示例                                 |
|----------------|---------------------------------------|---------------------------------------|
| `#{}` vs `${}` | `#{}`预编译防注入；`${}`拼接用于动态表名/排序字段（有注入风险） | `SqlExecutorMapper` 用 `${sql}` 需白名单校验 |
| 动态SQL          | `<if>`、`<where>`、`<set>`、`<foreach>`  | `EmployeeMapper.xml` 条件查询             |
| ResultMap      | 多表联查同名字段手动映射                          | 员工+部门+领导三表联查                          |
| 参数映射           | `@Param` 多参数、`POJO` 传参                | `EmployeeMapper.selectByCondition`    |

### 分页性能（阶段6重点）
- **offset 分页缺陷**：`LIMIT 100000, 20` 数据库要先扫描并丢弃前10万行，越翻越慢
- **游标分页（seek pagination）**：记录上一页最后一条的排序字段值，用 `WHERE id > lastId ORDER BY id LIMIT 20`，走索引直接定位，O(logN)

---

## 四、MyBatis Plus 知识地图

### 解决的问题
- **简单 CRUD 零 SQL**：继承 `BaseMapper` 获得 `selectById/insert/updateById/deleteById`
- **条件构造器**：`LambdaQueryWrapper` 类型安全，避免魔法字符串
- **分页**：注册分页插件后 `selectPage` 自动拼 LIMIT

### 与 MyBatis 的关系
```
MyBatis Plus = MyBatis + 增强工具包
```
MyBatis Plus **底层就是 MyBatis**，完全兼容 XML 原生写法。

### 适用场景对比
| 场景        | 推荐           | 原因                |
|-----------|--------------|-------------------|
| 单表增删改查    | MyBatis Plus | 零SQL、开发快          |
| 复杂多表查询    | 原生 MyBatis   | SQL 完全可控、调优方便     |
| 报表统计/窗口函数 | 原生 MyBatis   | 需手写复杂SQL          |
| 分页查询      | 均可           | Plus 自动、Native 手写 |

### 代码量对比（阶段7 `/api/users/compare`）
- 原生 MyBatis：接口声明 7 个方法 + XML 手写 7 段 SQL
- MyBatis Plus：继承 BaseMapper，0 方法 0 SQL

---

## 五、慢 SQL 分析与优化（阶段6）

> 📚 **完整实战教程见 `docs/mysql-index/`（推荐先读 `README.md`）**
> 那套教程用隔离的 `mall_user/mall_product/mall_order` 三张表（30万/20万/100万行）把下面每个知识点
> 都做成“错误 SQL → EXPLAIN → 建索引 → 再 EXPLAIN”的真实演练，含可执行脚本
> `docs/mysql-index/sql/01~06_*.sql`，并有配套演示接口 `/api/index-demo/*`（Controller 见
> `IndexDemoController.java`）。本小节保留速记版。

### 索引速记一：EXPLAIN 看 4 列
| 字段 | 含义 | 优化目标 |
|---------|---------------------------------------------------|---------|
| `type`  | 访问类型：ALL(full scan) < index < range < ref < const | 避免 ALL  |
| `key`   | 实际使用的索引（possible_keys 有 ≠ 用上）              | 优先走覆盖索引 |
| `rows`  | 预估扫描行数（对比表总行数看数量级）                   | 越小越好    |
| `Extra` | Using filesort / Using temporary 要警惕；**Using index=覆盖索引，不用回表** | 尽量避免 filesort |

### 索引速记二：索引失效 5 大场景
1. **where 字段计算**：`WHERE DATE(create_time)='2024-06-15'` → 改半开区间 `create_time >= ... AND create_time < ...`
2. **隐式类型转换**：字符串列 `WHERE phone = 13713529579`(数字) → 传字符串 `'13713529579'`
3. **like 前缀模糊**：`LIKE '%1371%'` 全表扫；只有 `LIKE '1371%'` 可走索引
4. **联合索引不满足最左前缀**：`(user_id,status,create_time)` 却没带 user_id → 整个索引用不上
5. **OR 条件**：`WHERE a=? OR b=?` 只要 b 无索引就退化全表扫 → 两边补索引或改 `UNION ALL`

### 索引速记三：联合索引最左前缀
`(dept_id, salary)` 联合索引：
- `WHERE dept_id = ? AND salary > ?` ✅ 走索引
- `WHERE salary > ?` ❌ 跳过了最左列 dept_id，索引失效
- 列序口诀：**等值条件在前（区分度高者优先）→ 范围条件 → ORDER BY 排序列放最后（可免 filesort）**

### 索引速记四：回表 / 覆盖索引
- 二级索引叶子只存“索引列 + 主键”，要返回的列不在索引里就要**回表**（每行一次主键随机IO）
- `Extra` 出现 `Using index` = 覆盖索引 = 不回表（`Using index condition`≠覆盖）
- 列表 SQL 按需写列，少用 `SELECT *`（强制回表 + 阻断覆盖索引）

### 索引速记五：慢 SQL 排查流程（阶段6实战版）
```
复现抓慢SQL → EXPLAIN(type/key/rows/Extra) → 定位（没索引 or 索引没对上SQL形态）
→ 改SQL 或 按最左前缀补索引 → 再 EXPLAIN 对比 rows → 慢日志看 Query_time/Rows_examined
→ 回归其它查询
```
> 完整事故演练（含真实慢日志行与前后耗时对比）：`docs/mysql-index/06-慢SQL排查实战.md`

---

## 六、AI 代码审查清单

拿到 AI 生成的 Java 后端代码，按以下顺序审查：

1. **SQL 合理性**
   - [ ] 是否 SELECT 了不需要的列（避免 `SELECT *`）
   - [ ] WHERE 是否使用了索引、有没有索引失效
   - [ ] 多表查询 JOIN 是否合理，有没有笛卡尔积
   - [ ] 分页是否用了深 offset（大数据量应游标分页）
   - [ ] 聚合是否会用错 HAVING/WHERE

2. **MyBatis 设计**
   - [ ] 动态 SQL 是否写对（`<if>` 条件、`<where>` 前缀）
   - [ ] `#{}` vs `${}` 是否安全（禁止随意拼接不可信输入）
   - [ ] ResultMap 字段映射是否完整
   - [ ] 批量操作是否用 `<foreach>` + 避免循环单条SQL

3. **三层结构**
   - [ ] Controller 是否只做参数接收和返回，无业务逻辑
   - [ ] Service 是否有事务注解 `@Transactional`（写操作）
   - [ ] 是否直接用 Entity 接收请求（应使用 DTO）
   - [ ] 异常是否被统一处理，是否泄露了 SQL 细节

---

## 七、面试高频题总结

### SQL 面试题
| 题目                | 一句话答案                                     |
|-------------------|-------------------------------------------|
| 窗口函数有哪几种？         | ROW_NUMBER 不并列、RANK 并列跳号、DENSE_RANK 并列不跳号 |
| 连续登录N天怎么做？        | 日期减ROW_NUMBER，差值相同即连续，再 GROUP BY          |
| 分组TopN为什么不用LIMIT？ | LIMIT不能分区，窗口函数 PARTITION BY 分区内排序         |
| WHERE和HAVING区别？   | WHERE过滤行（分组前），HAVING过滤分组（分组后）             |
| EXISTS和IN区别？      | EXISTS提前短路且对NULL安全，大表更推荐EXISTS            |

### MyBatis 面试题
| 题目             | 一句话答案                                                            |
|----------------|------------------------------------------------------------------|
| `#{}`和`${}`区别？ | `#{}`预编译占位防注入，`${}`字符串拼接有注入风险                                    |
| 一二级缓存区别？       | 一级缓存SqlSession级默认开启，二级缓存Mapper级需开启                               |
| 什么时候动态SQL？     | 条件不固定时用 if/where/foreach                                         |
| MyBatis执行流程？   | 加载配置→SqlSessionFactory→SqlSession→Executor→MappedStatement→执行→映射 |

### MyBatis Plus 面试题
| 题目                      | 一句话答案                              |
|-------------------------|------------------------------------|
| MyBatis和MyBatis Plus关系？ | Plus基于MyBatis增强，简单CRUD零SQL         |
| 什么时候用Plus什么时候用MyBatis？  | 简单CRUD用Plus，复杂查询/报表用MyBatis手写XML   |
| Wrapper是什么？             | 条件构造器，LambdaQueryWrapper类型安全无魔法字符串 |

---

## 八、项目结构

```
springMybatisSql/
├── pom.xml                                  # Maven 配置：Spring Boot 3.4.3 + JDK17 + MyBatis Plus 3.5.12
├── .gitignore                               # Git 忽略规则（target/、.idea/ 等）
├── PromptTemplate.md                        # 需求文档（本项目的设计依据）
├── 经典SQL35题.md                            # 原始经典题稿（本项目迁移来源）
├── docs/
│   └── 项目总结.md                           # 本文件：知识地图 + 面试总结 + 快速开始
├── sql/                                     # 【可选】SQL 脚本备份目录，可放建表SQL方便Navicat直连
└── src/
    ├── main/
    │   ├── java/com/dev/springmybatissql/
    │   │   ├── SpringMybatisSqlApplication.java   # 启动类
    │   │   ├── common/                            # 通用类
    │   │   │   ├── Result.java                    #   统一API响应体（code/message/data）
    │   │   │   └── ColumnRowResult.java           #   任意SQL查询结果（列名+行数据）
    │   │   ├── config/                            # 配置类
    │   │   │   ├── MybatisConfig.java             #   @MapperScan 扫描Mapper接口
    │   │   │   ├── MybatisPlusConfig.java         #   分页插件（阶段7用）
    │   │   │   └── SqlCaseInitializer.java        #   ★ 启动时写入22道SQL题目 + 自动生成标准结果
    │   │   ├── controller/                        # 控制层：接收HTTP请求，返回JSON
    │   │   │   ├── SqlCaseController.java         #   实验中心：题目列表/执行SQL/验证
    │   │   │   ├── EmployeeController.java        #   员工查询（阶段4调用链演示）
    │   │   │   ├── AnalysisController.java        #   数据分析（经典题+现代SQL扩展全套接口）
    │   │   │   └── UserController.java            #   用户CRUD（阶段7双实现对比）
    │   │   ├── dto/                               # 接收前端请求参数
    │   │   │   ├── ExecuteSqlRequest.java         #   执行SQL请求体
    │   │   │   └── UserDTO.java                   #   用户新增/修改参数（含校验）
    │   │   ├── entity/                            # 实体类（与数据库表一一对应）
    │   │   │   ├── User/Department/Employee/SalaryGrade/Product/Orders/
    │   │   │   ├── OrderItem/Role/OperationLog/LoginLog/
    │   │   │   ├── Student/Course/StudentCourse/SqlCase
    │   │   ├── mapper/                            # ★ Mapper接口层
    │   │   │   ├── SqlExecutorMapper.java         #   通用SQL执行器（${sql}动态执行）
    │   │   │   ├── SqlCaseMapper.java             #   题目CRUD（MyBatis Plus BaseMapper）
    │   │   │   ├── EmployeeMapper.java            #   员工查询（原生MyBatis + XML）
    │   │   │   ├── AnalysisMapper.java            #   数据分析SQL（原生MyBatis + XML）
    │   │   │   ├── UserNativeMapper.java          #   用户CRUD（原生MyBatis手写XML）
    │   │   │   └── UserPlusMapper.java            #   用户CRUD（MyBatis Plus零SQL）
    │   │   ├── service/                           # 业务层：接口 + 实现
    │   │   │   ├── SqlCaseService(Impl)           #   实验中心业务 + SQL安全校验 + 结果比对
    │   │   │   ├── EmployeeService(Impl)          #   员工业务
    │   │   │   ├── AnalysisService(Impl)          #   数据分析业务
    │   │   │   └── UserService(Impl)              #   用户业务（Plus版 vs Native版对比）
    │   │   └── vo/                                # 返回给前端展示的视图对象
    │   │       ├── EmployeeVO.java                #   员工+部门+领导合并视图
    │   │       ├── ExecuteSqlVO.java              #   SQL执行结果（列/行/耗时/验证）
    │   │       └── UserCompareVO.java             #   MyBatis vs Plus 对比结果
    │   └── resources/
    │       ├── application.yml                    # ★ 数据库连接 + MyBatis/MyBatis Plus配置
    │       ├── sql/
    │       │   ├── schema.sql                     #   ★ 建库建表（16张表，IF NOT EXISTS幂等）
    │       │   └── data.sql                       #   ★ 初始化数据（INSERT IGNORE幂等）
    │       ├── mapper/                            # ★ Mapper XML（原生MyBatis的SQL所在）
    │       │   ├── EmployeeMapper.xml             #   动态SQL/ResultMap/分页
    │       │   ├── AnalysisMapper.xml             #   经典35题+现代SQL全部语句
    │       │   └── UserNativeMapper.xml           #   用户CRUD手写SQL
    │       ├── static/
    │       │   └── index.html                     #   ★ SQL实验台前端页面（题目/编辑器/结果）
    │       └── templates/                         # Thymeleaf模板目录（本项目未用，保留）
    └── test/                                      # 单元测试（骨架）
```

> 带 `★` 的是学习时最值得重点阅读的文件。

---

## 九、推荐阅读学习顺序

按"从入口到深入"的顺序学习，每步对应本项目的对应文件：

### 第 1 步：跑通项目（1 小时）
1. 按 `八、快速开始` 把项目跑起来
2. 浏览器打开 http://localhost:8080 ，玩一遍 SQL 实验台
3. 感受完整闭环：左侧题目 → 中间写SQL → 右侧结果 → 自动验证

### 第 2 步：理解数据库模型（1 小时）
1. 阅读 `sql/schema.sql`，弄懂 16 张表的字段与关系（用 Navicat/DataGrip 画 ER 图更直观）
2. 阅读 `sql/data.sql` 的员工数据，对照经典 emp 表理解迁移逻辑（emp→employee、mgr→leader_id、sal→salary）
3. 重点理解：leader_id 自关联、dept_code 业务编码、salary_grade 薪资区间

### 第 3 步：打通 MyBatis 完整调用链（2 小时，核心）
按这个顺序追踪代码：
```
EmployeeController（入口）
   → EmployeeService / EmployeeServiceImpl（业务层）
   → EmployeeMapper（接口）
   → EmployeeMapper.xml（SQL）
   → MySQL
   
   开发顺序总结：
   1. 建表（SQL）
      ↓
   2. 实体类（Entity）          ← 先有"数据模型"
      ↓
   3. Mapper 接口               ← 定义"数据操作合同"
      ↓
   4. Mapper XML                ← 写 SQL，真正干活
      ↓
   5. Service（接口 + 实现）    ← 写"业务逻辑"，需要调 Mapper
      ↓
   6. Controller                ← 写"HTTP接口"，需要调 Service
   开发顺序：表→实体→Mapper→Service→Controller（自下往上搭）
   阅读顺序：Controller→Service→Mapper→XML→表（自上往下追）
```
1. 阅读 `EmployeeMapper.xml` 的 4 段 SQL：动态SQL（if/where）、ResultMap 三表联查、limit 分页、count
2. 用 IDA/端点逐个调用：`/api/employees`、`/api/employees/with-dept-leader`、`/api/employees/page`
3. 观察控制台打印的 SQL 日志，理解 MyBatis 预编译与参数映射

### 第 4 步：学习 SQL 实验中心（2 小时）
1. 阅读 `SqlCaseController → SqlCaseService(Impl) → SqlExecutorMapper`
2. 重点理解：
   - `SqlExecutorMapper` 为什么用 `${sql}`（SQL 语句不能预编译）→ 引出沙箱白名单校验
   - `SqlCaseServiceImpl.validateAndClean()` 如何防注入
   - `verifyResult()` 如何比对列名/行数/内容
3. 在实验台里挑 5 道入门题自己做一遍

### 第 5 步：吃透经典 35 题（3~4 小时，分多天）
1. 打开实验台，按难度从"入门→进阶→困难"逐题练习
2. 每题流程：看题目描述 → 自己写 SQL → 执行 → 看验证结果 → 对答案
3. 对照 `AnalysisMapper.xml` 学习标准写法；对照 `SqlCaseInitializer` 里的 description 学面试表达
4. 重点吃透这几类题（面试高频）：
   - 分组 Top1：每部门最高薪（题1）
   - 高于均值：部门平均/公司平均（题2、22）
   - 薪资区间：非等值连接（题3、22）
   - 自连接：领导关系（题14、15）
   - 不丢行：LEFT JOIN 驱动表（题16）
   - HAVING：部门人数（题17）
   - 子查询：ALL/NOT EXISTS（题25、35-1）

### 第 6 步：掌握现代 SQL 扩展（2 小时，面试加分）
1. 连续登录 N 天：`ROW_NUMBER` 差值法（实验台题40）
2. 分组 TopN：为什么 LIMIT 不行（题41、43）
3. 行列转换：CASE WHEN 条件聚合（题45）
4. rank vs dense_rank vs row_number（题46）
5. 慢 SQL 分析：用 `EXPLAIN` 验证第五节的知识点（建议自己在实验台执行 `EXPLAIN SELECT ...`）

### 第 7 步：MyBatis vs MyBatis Plus（1 小时）
1. 对比 `UserNativeMapper.xml`（手写7段SQL）和 `UserPlusMapper`（继承BaseMapper零SQL）
2. 调用 `/api/users/compare` 看实际返回对比
3. 结论：简单CRUD用Plus，复杂查询用原生MyBatis

### 第 8 步：复盘与面试（1 小时）
1. 重读本文件的二~七章（知识地图 + 面试题）
2. 用"七、面试高频题总结"自测，能不看答案复述即可
3. 用"六、AI代码审查清单"给自己代码（或AI生成代码）做一次体检

### 学习节奏建议
- 每天 1~2 小时，按上述 8 步 2~3 周学完
- **不要跳步**：第 3 步（MyBatis 调用链）是地基，务必先打通
- 每学完一步，在实验台"载入标准答案"复习一遍该题 SQL
- 想加新题练习时，按 `八-2 如何继续添加 SQL 题目` 操作

---

## 十、快速开始

### 1. 初始化项目（从零开始）

```bash
# ① 先改数据库账号密码（默认 root/root1234）
vim src/main/resources/application.yml
#    datasource.username / datasource.password 改成你本机 MySQL 的账号密码

# ② 启动应用（自动完成：建库 → 建16张表 → 初始化数据 → 写入22道SQL题目）
mvn spring-boot:run

# ③ 访问 SQL 实验中心页面
open http://localhost:8080
```

启动日志看到这两行即代表初始化成功：
```
Started SpringMybatisSqlApplication in 1.075 seconds
SQL 实验中心：成功初始化 22 道题目
```

> 幂等说明：schema.sql 用 `CREATE TABLE IF NOT EXISTS`、data.sql 用 `INSERT IGNORE`，
> 重启应用不会重复建表/插数据，不会报错。

### 2. 如何继续添加 SQL 题目（重点）

题目都维护在 `SqlCaseInitializer.buildCases()` 方法中（`src/main/java/com/dev/springmybatissql/config/SqlCaseInitializer.java`）。

**新增一道题的步骤：**

```java
// 在 buildCases() 方法 return list 之前，追加一条：
list.add(caseOf(
    50L,                      // ① id：不能与现有题目重复（现有 1~46）
    "每个部门薪资第二高的员工（自定义题）",   // ② 标题
    "【数据模型】employee、department\n【SQL分析】...\n【核心知识】...\n【面试表达】...", // ③ 题目描述
    "进阶",                   // ④ 难度：入门/进阶/困难
    "窗口函数/dense_rank",    // ⑤ 核心知识点(标签)
    "SELECT ... FROM ..."     // ⑥ 标准SQL答案（启动时自动执行，生成 expected_result）
));
```

**验证：**
- 重启应用，看到 `SQL 实验中心：成功初始化 N 道题目`（N=22+新增数）
- 或手动清空后重启（清空才会重新生成 expected_result）：
  ```sql
  DELETE FROM sql_case;
  ```

**注意：**
- 标准SQL必须能以当前数据库数据跑通（启动时真实执行生成 expected_result）
- 若 SQL 写错，启动日志会打印 `初始化题目失败 id=xx`，修正后 `DELETE FROM sql_case` 再重启即可
- 题目 id 建议跳号预留（如 50L），方便以后插入/重排

### 3. 常用接口

```bash
# 页面：SQL 实验台（方式2）
open http://localhost:8080

# 题目列表
curl http://localhost:8080/api/sql/cases

# 执行SQL并验证（方式1：控制台/命令行，caseId=1 为经典第1题）
curl "http://localhost:8080/api/sql/run?sql=SELECT%20*%20FROM%20employee&caseId=1"

# 数据分析接口示例
curl http://localhost:8080/api/analysis/top-salary-by-dept
```
